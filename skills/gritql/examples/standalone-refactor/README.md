# Standalone Grit refactor fixture

This example uses Grit's executable Markdown pattern format.

From this directory, with a compatible standalone Grit CLI:

```bash
grit patterns test --filter=rename_client_method
```

The example is intentionally narrow. Expand its fixture corpus and constraints before adapting it to a repository-wide migration.
